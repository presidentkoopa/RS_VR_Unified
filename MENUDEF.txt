// ============================================================
// from RS_Hands  (MENUDEF.txt)
// ============================================================
// Adds an RS Hands entry to the normal Options menu. AddOptionMenu appends to
// the engine's existing menu rather than replacing it, so this coexists with
// whatever else is loaded.
AddOptionMenu "OptionsMenu"
{
	Submenu "RS Hands", "RS_HandsOptions"
}

OptionMenu "RS_HandsOptions"
{
	Title "RS Hands"

	StaticText ""
	Option "Show Hands (psprite)",  "rs_hands",      "OnOff"
	// These and "World hands" below are ALTERNATIVES, not layers. Both on
	// draws two hands per controller in the same place.
	Option "Hands as fists",  "rs_hands_fists", "OnOff"
	Slider "Pose blend tics",  "rs_hands_blend", 0, 12, 0.5, 1
	Slider "Hand Scale",  "rs_hand_scale", 0.1, 3.0, 0.05, 2

	StaticText " "
	Submenu "Main Hand Placement", "RS_HandsMainPlacement"
	Submenu "Off Hand Placement",  "RS_HandsOffPlacement"

	StaticText " "
	StaticText "How a hand sits on the weapon it is", gold
	StaticText "gripping. By ROLE, not by weapon -- a", gold
	StaticText "grip is a grip on every gun.", gold

	StaticText " "
	StaticText "The two hands are one mesh mirrored, so a", gold
	StaticText "shared value moves them opposite ways.", gold
	StaticText "Each hand has its own set.", gold

	StaticText " "
	StaticText "Diagnostics", gold
	Option "Trace pose choice",   "rs_hands_debug",     "OnOff"
	Option "Trace renderer path", "vr_pose_debug",      "OnOff"
	Option "Force pose",          "rs_hands_forcepose", "RS_HandsPoseList"
	Slider "Browse source anim",  "rs_hands_srcframe", -1, 1277, 1, 0

	StaticText " "
	StaticText ""
	StaticText "WORLD-ACTOR HANDS -- the real architecture", 1
	// The psprite hands above are drawn relative to your eye and can never grab,
	// collide, or hold a world object. These ride the controllers through
	// MDL_FOLLOWMAINHAND/OFFHAND at draw rate, same as the world gun.
	Option "World hands",       "rs_handworld", "OnOff"

	StaticText "  poses", 1
	// The same twenty shapes the psprite hands use, now reachable on a world
	// actor. Frames 0-10 are the baked hand shapes; 11-19 are the manipulation
	// set, which lives at frames 1289-1297 and could not be addressed at all
	// until model frames could be named by number.
	Option "Force pose",        "rs_handworld_forcepose", "RS_HandsPoseList"
	Slider "Blend tics",        "rs_handworld_blend", 1, 20, 1, 0
	Option "Log pose changes",  "rs_handworld_debug", "OnOff"

	StaticText "  main hand", 1
	Slider "M Left / Right",   "rs_hw_main_ofs_x", -60, 60, 0.5, 2
	Slider "M Forward / Back", "rs_hw_main_ofs_y", -60, 60, 0.5, 2
	Slider "M Down / Up",      "rs_hw_main_ofs_z", -60, 60, 0.5, 2
	Slider "M Yaw",            "rs_hw_main_yaw",  -180, 180, 1, 1
	Slider "M Pitch",          "rs_hw_main_pitch",-180, 180, 1, 1
	Slider "M Roll",           "rs_hw_main_roll", -180, 180, 1, 1
	Slider "M Scale",          "rs_hw_main_scale", 0.05, 5.0, 0.05, 2

	StaticText "  off hand", 1
	Slider "O Left / Right",   "rs_hw_off_ofs_x", -60, 60, 0.5, 2
	Slider "O Forward / Back", "rs_hw_off_ofs_y", -60, 60, 0.5, 2
	Slider "O Down / Up",      "rs_hw_off_ofs_z", -60, 60, 0.5, 2
	Slider "O Yaw",            "rs_hw_off_yaw",  -180, 180, 1, 1
	Slider "O Pitch",          "rs_hw_off_pitch",-180, 180, 1, 1
	Slider "O Roll",           "rs_hw_off_roll", -180, 180, 1, 1
	Slider "O Scale",          "rs_hw_off_scale", 0.05, 5.0, 0.05, 2

	SafeCommand "Reset world hands", "resetcvar rs_hw_main_ofs_x rs_hw_main_ofs_y rs_hw_main_ofs_z rs_hw_main_yaw rs_hw_main_pitch rs_hw_main_roll rs_hw_main_scale rs_hw_off_ofs_x rs_hw_off_ofs_y rs_hw_off_ofs_z rs_hw_off_yaw rs_hw_off_pitch rs_hw_off_roll rs_hw_off_scale"

}

// WHICH SPACE THE REACH NUMBERS ARE IN -- see CVARINFO. Two readings of the
// same six cvars, both defensible from the source, only one of them true in a
// headset. The names say what you would SEE, because that is the evidence the
// person at the menu actually has.
OptionValue "RS_GrabScaleSpace"
{
	0, "Map units (34 = 1m)"
	1, "Drawn mesh (1 = 1m)"
}

// Named by the MOVEMENT, not by the joint, because the movement is what the
// person choosing has to picture themselves doing in a chair.
OptionValue "RS_DGrabFlickMode"
{
	0, "Pull hand back (shoulder)"
	1, "Tip wrist up (seated)"
}

// Named so a forced pose reads as a hand shape rather than a frame number.
OptionValue "RS_HandsPoseList"
{
	-1, "Controllers"
	0,  "0 - Open"
	1,  "1 - Grip (3-4-5)"
	2,  "2 - Trigger (index)"
	3,  "3 - Fist"
	4,  "4 - Pinch"
	5,  "5 - Thumb out"
	6,  "6 - Grip + fire"
	7,  "7 - Grip, thumb up"
	8,  "8 - Finger on trigger"
	9,  "9 - Finger on trigger, thumb up"
	10, "10 - Fire, thumb up"
	11, "11 - Hold a round"
	12, "12 - Hold a shell"
	13, "13 - Inserting"
	14, "14 - Hold the slide"
	15, "15 - Hold a magazine"
	16, "16 - Hold a foregrip"
	17, "17 - Hold the forend"
	18, "18 - Reach"
	19, "19 - Support (round the firing hand)"
}

OptionMenu "RS_HandsMainPlacement"
{
	Title "Main Hand"

	StaticText ""
	StaticText "Position", gold
	Slider "Left / Right",   "vr_hand_ofs_x", -60, 60, 0.5, 2
	Slider "Forward / Back", "vr_hand_ofs_y", -60, 60, 0.5, 2
	Slider "Down / Up",      "vr_hand_ofs_z", -60, 60, 0.5, 2

	StaticText " "
	StaticText "Rotation", gold
	Slider "Yaw",   "vr_hand_yaw",   -180, 180, 5, 1
	Slider "Pitch", "vr_hand_pitch", -180, 180, 5, 1
	Slider "Roll",  "vr_hand_roll",  -180, 180, 5, 1

	StaticText " "
	SafeCommand "Reset main hand", "resetcvar vr_hand_ofs_x vr_hand_ofs_y vr_hand_ofs_z vr_hand_yaw vr_hand_pitch vr_hand_roll"
}

OptionMenu "RS_HandsOffPlacement"
{
	Title "Off Hand"

	StaticText ""
	StaticText "Position", gold
	Slider "Left / Right",   "vr_offhand_ofs_x", -60, 60, 0.5, 2
	Slider "Forward / Back", "vr_offhand_ofs_y", -60, 60, 0.5, 2
	Slider "Down / Up",      "vr_offhand_ofs_z", -60, 60, 0.5, 2

	StaticText " "
	StaticText "Rotation", gold
	Slider "Yaw",   "vr_offhand_yaw",   -180, 180, 5, 1
	Slider "Pitch", "vr_offhand_pitch", -180, 180, 5, 1
	Slider "Roll",  "vr_offhand_roll",  -180, 180, 5, 1

	StaticText " "
	SafeCommand "Reset off hand", "resetcvar vr_offhand_ofs_x vr_offhand_ofs_y vr_offhand_ofs_z vr_offhand_yaw vr_offhand_pitch vr_offhand_roll"
}





// Hand grabbing gets its own Options entry. It is not a property of any weapon
// -- it is how picking anything up works, and it is the thing most likely to be
// tuned repeatedly.
AddOptionMenu "OptionsMenu"
{
	Submenu "Hand Grab", "RS_GrabMenu"
}

OptionMenu "RS_GrabMenu"
{
	Title "Hand Grab"

	StaticText ""
	Option "Grabbing (master)",         "rs_grab", "OnOff"
	StaticText ""
	Submenu "Reach and aim",            "RS_GrabReachMenu"
	Submenu "Holding",                  "RS_GrabHoldMenu"
	Submenu "Distance grab",            "RS_GrabDistMenu"
	Submenu "Flick and throw",          "RS_GrabThrowMenu"
	Submenu "Using items",              "RS_GrabUseMenu"
	Submenu "Feedback",                 "RS_GrabFeedMenu"
	Submenu "What is grabbable",        "RS_GrabWhatMenu"
}

//======================================================================
OptionMenu "RS_GrabReachMenu"
{
	Title "Reach and aim"

	StaticText ""
	StaticText "The oval IS the reach", 1
	// Drawn from the same three cvars RS_Reach tests against, so the gauge
	// cannot drift from the volume. The captions bind to the cvar that draws
	// that axis, not to the letter in its name -- the engine decides which
	// letter is which, and it says x is sideways.
	StaticText "34 units = 1 metre.  A fist is about 2 units.", 1
	StaticText "SIZE first, then the three axes for shape.", 1

	StaticText ""
	StaticText "If the oval looks right but nothing grabs --", 1
	StaticText "or it grabs fine and the oval is metres wide --", 1
	StaticText "this is the switch. Flip it and look again.", 1
	Option "Sizes are in",              "rs_grab_scale_space", "RS_GrabScaleSpace"

	StaticText "Main hand", 1
	Slider "  OVERALL SIZE",   "rs_grab_m_scale", 0.005, 4.0, 0.005, 3
	Slider "  Along fingers",  "rs_grab_m_scale_y", 0.2, 10, 0.1, 1
	Slider "  Across palm",    "rs_grab_m_scale_x", 0.2, 10, 0.1, 1
	Slider "  Through hand",   "rs_grab_m_scale_z", 0.2, 10, 0.1, 1
	StaticText "Off hand", 1
	Slider "  OVERALL SIZE",   "rs_grab_o_scale", 0.005, 4.0, 0.005, 3
	Slider "  Along fingers",  "rs_grab_o_scale_y", 0.2, 10, 0.1, 1
	Slider "  Across palm",    "rs_grab_o_scale_x", 0.2, 10, 0.1, 1
	Slider "  Through hand",   "rs_grab_o_scale_z", 0.2, 10, 0.1, 1

	StaticText ""
	StaticText "Where it sits on the hand -- live", 1
	StaticText "With world hands off, the reach centres on the", 1
	StaticText "CONTROLLER, which is your wrist. Push it forward.", 1
	Slider "M Forward / back", "rs_grab_m_ofs_y", -20, 20, 0.25, 2
	Slider "M Left / right",   "rs_grab_m_ofs_x", -20, 20, 0.25, 2
	Slider "M Down / up",      "rs_grab_m_ofs_z", -20, 20, 0.25, 2
	Slider "O Forward / back", "rs_grab_o_ofs_y", -20, 20, 0.25, 2
	Slider "O Left / right",   "rs_grab_o_ofs_x", -20, 20, 0.25, 2
	Slider "O Down / up",      "rs_grab_o_ofs_z", -20, 20, 0.25, 2

	StaticText ""
	SafeCommand "Reset", "resetcvar rs_grab_scale_space rs_grab_m_ofs_x rs_grab_m_ofs_y rs_grab_m_ofs_z rs_grab_m_scale rs_grab_m_scale_x rs_grab_m_scale_y rs_grab_m_scale_z rs_grab_o_ofs_x rs_grab_o_ofs_y rs_grab_o_ofs_z rs_grab_o_scale rs_grab_o_scale_x rs_grab_o_scale_y rs_grab_o_scale_z"
}

//======================================================================
OptionMenu "RS_GrabHoldMenu"
{
	Title "Holding"

	StaticText ""
	StaticText "A grabbed thing stays in your hand instead of", 1
	StaticText "going straight into inventory.", 1
	Option "Squeeze toggles",           "rs_hold_toggle",  "OnOff"
	Option "Second hand can join",      "rs_hold_twohand", "OnOff"
	Option "Second hand can take",      "rs_hold_pass",    "OnOff"

	StaticText ""
	StaticText "How far it may lag behind the palm before", 1
	StaticText "your hand loses it. 34 units = 1 metre.", 1
	Slider "Let go at",                 "rs_hold_break", 0, 120, 2, 0
	Option "Turn with wrist",           "rs_hold_rotate", "OnOff"
	Slider "Weight feedback",           "rs_hold_haptic", 0, 1.5, 0.05, 2
	StaticText ""
	StaticText "Walking into a pickup collects it long before", 1
	StaticText "your hand gets near it. On = you must reach.", 1
	Option "Must reach for pickups",    "rs_grab_nowalkover", "OnOff"
}

//======================================================================
OptionMenu "RS_GrabDistMenu"
{
	Title "Distance grab"

	StaticText ""
	StaticText "Point and squeeze. Only used when your palm", 1
	StaticText "is empty -- something you are touching wins.", 1
	Option "Distance grabbing",         "rs_dgrab", "OnOff"

	StaticText ""
	StaticText "Reach and spread are separate on purpose: how", 1
	StaticText "FAR and how PRECISELY are different questions.", 1
	Slider "  How far (34 = 1m)",       "rs_dgrab_reach",  34, 2048, 17, 0
	Slider "  How tight to point",      "rs_dgrab_spread", 1, 45, 0.5, 1
	Option "  Needs line of sight",     "rs_dgrab_sight", "OnOff"

	StaticText ""
	StaticText "Speed, not duration -- flight time comes from", 1
	StaticText "the distance, so near and far arrive alike.", 1
	Slider "  Speed (m/s)",             "rs_dgrab_speed",  2, 60, 1, 0
	Slider "  Never quicker than",      "rs_dgrab_time_min", 1, 30, 1, 0
	Slider "  Never slower than",       "rs_dgrab_time_max", 10, 140, 5, 0

	StaticText ""
	StaticText "1 = constant speed. Higher starts slow and", 1
	StaticText "speeds up, which is what a PULL feels like.", 1
	Slider "  Acceleration",            "rs_dgrab_accel",  1, 4, 0.1, 1

	StaticText ""
	StaticText "Arc height as a fraction of the distance, so", 1
	StaticText "the shape holds at every range.", 1
	Slider "  Arc height",              "rs_dgrab_arc",    0, 0.6, 0.01, 2

	StaticText ""
	StaticText "Turns end over end over the whole flight, so", 1
	StaticText "near and far tumble alike. Whole numbers land", 1
	StaticText "square; 0 is off. A barrel wants about 2.", 1
	Slider "  Tumble in flight",        "rs_dgrab_tumble", 0, 6, 0.5, 1

	StaticText ""
	StaticText "Miss the catch and it just lands at your feet.", 1
	StaticText "Above zero instead makes a miss cost you: a", 1
	StaticText "pickup gets used up, anything else hits you.", 1
	Slider "  Cost of a missed catch",   "rs_dgrab_impact", 0, 40, 1, 0
}

//======================================================================
OptionMenu "RS_GrabThrowMenu"
{
	Title "Flick and throw"

	StaticText ""
	StaticText "Open your hand at something locked and it comes", 1
	StaticText "-- no button. Tipping the wrist is the seated", 1
	StaticText "gesture: it needs no shoulder, so it works with", 1
	StaticText "your elbow resting on an armrest.", 1
	Option "Flick to pull",             "rs_dgrab_flick", "OnOff"
	Option "  Gesture",                 "rs_dgrab_flick_mode", "RS_DGrabFlickMode"
	Slider "  Wrist speed (deg/tic)",   "rs_flick_pitch", 1.0, 20, 0.5, 1
	Slider "  Hand speed (m/s)",        "rs_flick_speed", 0.5, 8, 0.1, 1

	StaticText ""
	StaticText "Letting go and throwing are the same act. Speed", 1
	StaticText "is the PEAK of the last 180ms, not the instant", 1
	StaticText "of release -- your arm is already slowing by then.", 1
	Option "Throwing",                  "rs_throw", "OnOff"
	Slider "  Strength",                "rs_throw_scale", 0.1, 3, 0.05, 2
	Slider "  Drop below (m/s)",        "rs_throw_min", 0, 5, 0.1, 1
}

//======================================================================
OptionMenu "RS_GrabUseMenu"
{
	Title "Using items"

	StaticText ""
	StaticText "Bring a held item to your face to use it.", 1
	StaticText "Not your body -- a held thing is already inside", 1
	StaticText "your own collision cylinder.", 1
	Option "Use at face",               "rs_use_at_face", "OnOff"
	Slider "  How close (34 = 1m)",     "rs_use_face_reach", 4, 40, 1, 0

	StaticText ""
	StaticText "Nobody brings a shell box to their mouth, so", 1
	StaticText "caught ammo just resolves.", 1
	StaticText "The pouch option does NOTHING yet -- the pouch", 1
	StaticText "it would route to does not exist.", 1
	Option "Ammo needs the pouch",      "rs_use_ammo_pouch", "OnOff"
}

//======================================================================
OptionMenu "RS_GrabFeedMenu"
{
	Title "Feedback"

	StaticText ""
	StaticText "considered = neutral / orange", 1
	StaticText "in your hand = orange / green", 1
	StaticText "ready to use at your face = green / cyan", 1
	Option "Flash the target",          "rs_grab_lightup", "OnOff"
	Slider "  Flash speed (tics)",      "rs_grab_flash", 4, 70, 2, 0

	StaticText ""
	Submenu "The aiming ray",           "RS_GrabRayMenu"

	StaticText ""
	StaticText "HAPTICS HAVE NEVER BEEN PROVEN IN THIS BUILD.", 1
	StaticText "Turn on vr_haptic_debug and read [HAPTIC]:", 1
	StaticText "nothing printed = the call never left.", 1
	Option "Tick at face range",        "rs_use_face_haptic", "OnOff"
	Slider "  Strength",                "rs_use_face_haptic_amp", 0.05, 1.0, 0.05, 2

	StaticText ""
	StaticText "Gauges. The drawn volume IS the tested volume.", 1
	Option "Show the reach volume",     "rs_grabviz", "OnOff"
	Option "Only when something is in", "rs_grabviz_onlywhenhot", "OnOff"
	Option "Show what it is on",        "rs_volviz", "OnOff"
	Slider "Opacity",                   "rs_grabviz_alpha", 0.05, 1.0, 0.05, 2

	StaticText ""
	StaticText "The face sphere sits where you cannot look at", 1
	StaticText "it. For dialling the number, then switch it off.", 1
	Option "Show the face volume",      "rs_use_face_viz", "OnOff"

	StaticText ""
	Option "Log holding and grabbing",  "rs_hold_debug",   "OnOff"
}

//======================================================================
//======================================================================
OptionMenu "RS_GrabRayMenu"
{
	Title "The aiming ray"

	StaticText ""
	Option "Draw the ray at all",       "rs_dgrab_beam", "OnOff"
	Slider "Edge softness",             "rs_beam_soft", 0, 1, 0.05, 2

	StaticText ""
	StaticText "IDLE -- pointing at nothing.", 1
	StaticText "Off is a real choice: a permanent line in your", 1
	StaticText "vision may be worse than the aiming it solves.", 1
	Option "  Draw when idle",          "rs_beam_idle_show", "OnOff"
	ColorPicker "  Colour",             "rs_beam_idle_color"
	Slider "  Opacity",                 "rs_beam_idle_alpha", 0, 1, 0.05, 2
	Slider "  Thickness",               "rs_beam_idle_thick", 0.05, 4, 0.05, 2
	StaticText "0 = as far as you could actually pull from.", 1
	Slider "  Length",                  "rs_beam_idle_len", 0, 2048, 32, 0

	StaticText ""
	StaticText "CANDIDATE -- something grabbable under it.", 1
	ColorPicker "  Colour",             "rs_beam_hot_color"
	Slider "  Opacity",                 "rs_beam_hot_alpha", 0, 1, 0.05, 2
	Slider "  Thickness",               "rs_beam_hot_thick", 0.05, 4, 0.05, 2

	StaticText ""
	StaticText "LOCKED -- your fist is closed on it.", 1
	ColorPicker "  Colour",             "rs_beam_lock_color"
	Slider "  Opacity",                 "rs_beam_lock_alpha", 0, 1, 0.05, 2
	Slider "  Thickness",               "rs_beam_lock_thick", 0.05, 4, 0.05, 2
	StaticText "Drawn from the OBJECT to your hand, so the pulse,", 1
	StaticText "the taper and the flare all run toward you.", 1
	Option "  Reel the pulse inward",   "rs_dgrab_beam_reel", "OnOff"

	StaticText ""
	StaticText "THE LINE ITSELF. Level-wide -- these govern every", 1
	StaticText "beam in the level, not just this one.", 1
	StaticText "Everything directional runs start to end.", 1
	Slider "Pulse speed",               "rs_beam_scroll",  0, 30, 0.5, 1
	Slider "Pulse depth",               "rs_beam_depth",   0, 1, 0.05, 2
	Slider "Glow in the air",           "rs_beam_airglow", 0, 3, 0.1, 1
	Slider "Taper",                     "rs_beam_taper",   0, 1, 0.05, 2
	Slider "Flare at the end",          "rs_beam_flare",   0, 5, 0.1, 1
	Slider "Halo",                      "rs_beam_glow",    0, 2, 0.05, 2
	Slider "Catches in fog",            "rs_beam_fog",     0, 2, 0.05, 2

	StaticText ""
	SafeCommand "Reset the ray", "resetcvar rs_beam_soft rs_beam_idle_show rs_beam_idle_color rs_beam_idle_alpha rs_beam_idle_thick rs_beam_idle_len rs_beam_hot_color rs_beam_hot_alpha rs_beam_hot_thick rs_beam_lock_color rs_beam_lock_alpha rs_beam_lock_thick rs_dgrab_beam_reel rs_beam_airglow rs_beam_scroll rs_beam_depth rs_beam_taper rs_beam_flare rs_beam_glow rs_beam_fog"
}

OptionMenu "RS_GrabWhatMenu"
{
	Title "What is grabbable"

	StaticText ""
	StaticText "The full table is in rs_grabpolicy.zs. These are", 1
	StaticText "the answers worth changing mid-session.", 1
	Option "Barrels",                   "rs_grab_barrels", "OnOff"
	Option "Corpses",                   "rs_grab_corpses", "OnOff"
	Option "Weapons on the floor",      "rs_grab_weapons", "OnOff"

	StaticText ""
	StaticText "A LIVE monster is never grabbable and has no", 1
	StaticText "switch. Closing a hand on one is a way of", 1
	StaticText "deleting monsters, not an interaction.", 1
}


// ============================================================
// from RS_Holsters  (MENUDEF.txt)
// ============================================================
// Hooked directly into the root options menu rather than nested under any
// other mod's submenu -- this pk3's menu presence does not depend on any
// other mod's menu structure. The holsters need SOME weapon pack loaded to
// have anything to show, but never reference RS_Main or any other specific
// one -- doSwap/findFist/ShowWeapon (RS_Holsters.zs / RS_HolsterProp.zs)
// all resolve weapons generically off the player's own inventory and
// GetClassName(), never a hardcoded class.
OptionValue "RS_HolsterSoundStyle"
{
	0, "Holster clunk"
	1, "Ready-to-fire beep"
}

OptionValue "RS_HolsterMarkerShape"
{
	0, "Bracket reticle"
	1, "Wireframe sphere"
}

OptionValue "RS_HolsterMarkerColor"
{
	0, "Default"
	1, "Blue"
	2, "Red"
	3, "Gold"
	4, "Purple"
	5, "Orange"
	6, "Green"
	7, "Cyan"
	8, "Pink"
}

OptionValue "RS_HolsterActiveCount"
{
	2, "2 -- Hip"
	4, "4 -- + Shoulder"
	6, "6 -- + Pectoral"
	8, "8 -- + Hip (dual)"
}

AddOptionMenu "OptionsMenu"
{
	StaticText " "
	Submenu "Body Holsters", "RS_HolsterOptions"
}

// ---------------------------------------------------------------------
// Body Holsters. Split into subpages rather than one long scroll -- this
// had grown to 7 sections in one page, which is a lot to scroll through in
// VR for a single setting. Diagnostics stayed on the root page rather than
// getting its own subpage: one SafeCommand is not worth a whole submenu.
// ---------------------------------------------------------------------
OptionMenu "RS_HolsterOptions"
{
	Title "Body Holsters"
	StaticText " "

	Submenu "Placement & Profiles", "RS_HolsterPlacement"
	Submenu "Appearance & Sound",   "RS_HolsterAppearance"
	Submenu "Timing & Grip",        "RS_HolsterTiming"
	StaticText " "

	StaticText "Diagnostics", "Gold"
	SafeCommand "Debug dump to console", "netevent rs-holster-debug"
	// ADDED 2026-08-26. Everything this switch covers used to run
	// unconditionally in the shipped build -- see CVARINFO.txt for the full
	// list. The manual dump above and the range haptic are NOT affected.
	Option "Verbose console logging",    "rs_holster_verbose", "OnOff"
	StaticText "Range enter/leave lines, and the automatic orientation", "DarkGray"
	StaticText "dump after every store.", "DarkGray"
	StaticText "Off in a normal build; turn on for a tuning session.", "DarkGray"
}

// SafeCommand rather than Command for the destructive/modal ones: edit
// mode and reset both change state you would not want triggered by a
// misread controller press while scrolling a menu in VR.
OptionMenu "RS_HolsterPlacement"
{
	Title "Placement & Profiles"
	StaticText " "

	StaticText "Placement", "Gold"
	Option "How many holsters",         "rs_holster_active_count", "RS_HolsterActiveCount"
	Option "Chest ammo pouch (for RS_Reload)", "rs_holster_pouch_enabled", "OnOff"
	// Filed here under the pouch's own on/off rather than over in Appearance
	// with the other marker-look rows, because it is meaningless without the
	// row above it: the user who just switched the pouch on is exactly the
	// user this should be sitting in front of. Pouch only -- the eight torso
	// holsters have no equivalent and this row does not claim otherwise.
	Option "  Pouch glows when low on ammo", "rs_holster_pouch_glow", "OnOff"
	StaticText "Invisible while stocked, fades up as the gun in your", "DarkGray"
	StaticText "hand runs dry -- 20% ammo left reads as barely there,", "DarkGray"
	StaticText "10% as half lit, 1% as almost solid. A hand reaching", "DarkGray"
	StaticText "for the pouch still lights it however full you are.", "DarkGray"
	SafeCommand "Edit Mode -- grab spheres and move them", "netevent rs-holster-edit"
	SafeCommand "Print tuned table to console",            "netevent rs-holster-table"
	SafeCommand "Reset offsets to defaults",               "netevent rs-holster-reset"
	SafeCommand "Recalibrate standing height",             "netevent rs-holster-recalibrate"
	StaticText " "

	// A profile is the CURRENT edit-table -- tune it with Edit Mode above,
	// then save it under whichever posture you tuned it for. Loading takes
	// effect immediately; nothing here needs a rebuild or a relaunch.
	StaticText "Profiles", "Gold"
	SafeCommand "Save current as STANDING", "netevent rs-holster-save-standing"
	SafeCommand "Load STANDING",            "netevent rs-holster-load-standing"
	SafeCommand "Save current as SEATED",   "netevent rs-holster-save-seated"
	SafeCommand "Load SEATED",              "netevent rs-holster-load-seated"
	StaticText " "
	SafeCommand "SWITCH seated/standing",   "netevent rs-holster-switch-profile"
}

OptionMenu "RS_HolsterAppearance"
{
	Title "Appearance & Sound"
	StaticText " "

	StaticText "Display", "Gold"
	Option "Show holster markers",      "rs_holster_markers", "OnOff"
	StaticText "The ring or bracket marking where each holster is.", "DarkGray"
	Option "Show stored weapons",       "rs_holster_props", "OnOff"
	StaticText "The weapon model parked in an occupied holster.", "DarkGray"
	StaticText " "
	Option "Marker shape",              "rs_holster_marker_shape", "RS_HolsterMarkerShape"
	Option "Marker color (cold)",       "rs_holster_marker_color", "RS_HolsterMarkerColor"
	Option "Marker color (hot)",        "rs_holster_marker_color_hot", "RS_HolsterMarkerColor"
	Slider "Marker scale",              "rs_holster_marker_scale", 0.3, 3.0, 0.1, 2
	Slider "Stored weapon size",         "rs_holster_prop_scale", 0.1, 4.0, 0.05, 2
	Slider "Fill amount (measured models)", "rs_holster_prop_fill", 0.1, 1.0, 0.05, 2
	Slider "Display size (measured models)", "rs_holster_prop_visual_radius", 2.0, 24.0, 0.5, 1
	Slider "Stored weapon yaw",         "rs_holster_prop_yaw", -180.0, 180.0, 5.0, 0
	Slider "Stored weapon pitch",       "rs_holster_prop_pitch", -180.0, 180.0, 5.0, 0
	Slider "Stored weapon roll",        "rs_holster_prop_roll",  -180.0, 180.0, 5.0, 0
	Slider "Stored weapon up/down",     "rs_holster_prop_up",  -12.0, 12.0, 0.5, 1
	Slider "Stored weapon fwd/back",    "rs_holster_prop_fwd", -12.0, 12.0, 0.5, 1
	Slider "Stored weapon side",        "rs_holster_prop_side", -12.0, 12.0, 0.5, 1
	StaticText " "

	StaticText "Sound", "Gold"
	Option "Store/draw confirm sound", "rs_holster_sound", "OnOff"
	Option "Confirm sound style",      "rs_holster_sound_style", "RS_HolsterSoundStyle"
}

OptionMenu "RS_HolsterTiming"
{
	Title "Timing & Grip"
	StaticText " "

	StaticText "Timing", "Gold"
	// Uses GZDoom's own instant-switch mechanism, scoped to just the swap a
	// holster triggers -- number-key and wheel switching keep their normal
	// animated speed either way. Off restores the real ~16-tic lower/raise
	// and a longer debounce between store/draw presses.
	Option "Instant store/draw", "rs_holster_instant_switch", "OnOff"
	StaticText " "

	StaticText "Grip", "Gold"
	// When on, a hand inside a holster spends its grip on the holster and
	// nothing else -- stabilize and the shift layer both stand down for
	// that hand. Off leaves grip alone entirely and the bound holster keys
	// are the only way in. This is an ENGINE cvar (DoomXR), not this pk3's
	// own -- referencing it here does not require this pk3 to own or
	// declare it. Two handed weapons / stabilize distance used to be listed
	// here too, but those are general VR control settings, not anything
	// holster-specific -- they already have a home in the engine's own
	// options menu, and duplicating them here was just two ways to find the
	// same setting.
	Option "Grip works holsters",       "vr_holster_use_grip", "OnOff"
}


// ============================================================
// from RS_HardPoints  (MENUDEF.txt)
// ============================================================
// Hooked directly into the root options menu rather than nested under any
// other mod's submenu -- this pk3's menu presence does not depend on any
// other mod's menu structure. The hardpoints need SOME weapon pack loaded
// to have anything to show, but never reference a specific one.
OptionValue "RS_HardPointSoundStyle"
{
	0, "Holster clunk"
	1, "Ready-to-fire beep"
}

OptionValue "RS_HardPointMarkerShape"
{
	0, "Bracket reticle"
	1, "Wireframe sphere"
	2, "Wireframe diamond"
}

OptionValue "RS_HardPointMarkerColor"
{
	0, "Default"
	1, "Blue"
	2, "Red"
	3, "Gold"
	4, "Purple"
	5, "Orange"
	6, "Green"
	7, "Cyan"
	8, "Pink"
}

// How many of ONE arm's three wrist mounts are live. Paired with a plain
// on/off per arm rather than folded into it -- see CVARINFO: the owner asked
// for a switch and a count, so that flicking the arm off does not lose the
// count and changing the count does not switch the arm on.
//
// REPLACES "RS_HardPointArmActiveCount" (Off / Forearm only / Wrist only /
// All six). The forearm tier is deferred as a feature, so its rows are gone
// rather than sitting in the menu doing nothing; the wrist tier is now three
// mounts on EACH arm instead of three on one.
OptionValue "RS_HardPointWristCount"
{
	1, "1"
	2, "2"
	3, "3"
}

AddOptionMenu "OptionsMenu"
{
	StaticText " "
	Submenu "Arm Hardpoints", "RS_HardPointOptions"
}

// The arm-anchored wrist hardpoints -- three around each wrist, six total.
// SafeCommand rather than Command for the destructive/modal ones: edit mode
// and reset both change state you would not want triggered by a misread
// controller press while scrolling a menu in VR.
OptionMenu "RS_HardPointOptions"
{
	Title "Arm Hardpoints"
	StaticText " "

	// EXACTLY THE SHAPE THE OWNER SPECIFIED (2026-08-26): a switch and a
	// count, per hand, and nothing else. Three wrist mounts per arm, six
	// total -- the same six anchors the single-arm layout had, re-laid out
	// rather than added to.
	//
	// NO FOREARM ROWS. That tier is deferred as a feature, not cancelled
	// (it was going to hold utility items -- flares, shields, usable
	// inventory -- and the content model for that is not built). Everything
	// learned about its placement is preserved in the DEFERRED block on
	// RS_HardPoints.zs' constants; what is NOT preserved is a menu row for
	// something that does nothing.
	StaticText "Mounts -- MAIN hand", "Gold"
	Option "Wrist slots",       "rs_hardpoint_wrist_main", "OnOff"
	Option "How many",          "rs_hardpoint_wrist_main_count", "RS_HardPointWristCount"
	StaticText " "

	StaticText "Mounts -- OFF hand", "Gold"
	Option "Wrist slots",       "rs_hardpoint_wrist_off", "OnOff"
	Option "How many",          "rs_hardpoint_wrist_off_count", "RS_HardPointWristCount"
	StaticText " "

	StaticText "Placement", "Gold"
	SafeCommand "Edit Mode -- grab spheres and move them", "netevent rs-hardpoint-edit"
	SafeCommand "Print tuned table to console",            "netevent rs-hardpoint-table"
	SafeCommand "Reset offsets to defaults",               "netevent rs-hardpoint-reset"
	// ADDED 2026-08-26. NetworkProcess has handled this netevent since the
	// split, with no alias and no menu row anywhere -- reachable only by
	// typing it into the console.
	SafeCommand "Recalibrate standing height",             "netevent rs-hardpoint-recalibrate"
	StaticText " "

	// The CURRENT edit-table -- tune it with Edit Mode above, then save it,
	// so the tuning survives a restart without a rebuild. Loading takes
	// effect immediately. One slot only: RS_Holsters keeps a seated and a
	// standing profile because its anchors hang off a body, and these are
	// bolted to a tracked controller, where posture is not a variable.
	//
	// ONE SLOT still covers BOTH ARMS. Each arm has its own layout -- the
	// mounts sit relative to whichever controller wears them, so one shared
	// table could not serve both -- but the two live in separate key ranges
	// inside the same file ("m0_".."m2_" and "o0_".."o2_", see profileKey),
	// so saving writes both arms and loading restores both. A layout saved
	// before 2026-08-26 uses the old flat "h0_".."h5_" keys and will simply
	// not be found; every field falls back to the new defaults, which is the
	// honest outcome given indices 0-2 used to mean the forearm row.
	StaticText "Saved Layout", "Gold"
	SafeCommand "Save tuned layout", "netevent rs-hardpoint-save-layout"
	SafeCommand "Load tuned layout", "netevent rs-hardpoint-load-layout"
	StaticText " "

	StaticText "Appearance", "Gold"
	Option "Show hardpoint markers",    "rs_hardpoint_markers", "OnOff"
	StaticText "The ring or bracket marking where each mount is.", "DarkGray"
	Option "Show stored items",         "rs_hardpoint_props", "OnOff"
	StaticText "The item model parked in an occupied mount.", "DarkGray"
	Option "Marker shape",              "rs_hardpoint_marker_shape", "RS_HardPointMarkerShape"
	Option "Marker color (cold)",       "rs_hardpoint_marker_color", "RS_HardPointMarkerColor"
	Option "Marker color (hot)",        "rs_hardpoint_marker_color_hot", "RS_HardPointMarkerColor"
	Slider "Marker scale",              "rs_hardpoint_marker_scale", 0.3, 3.0, 0.1, 2
	Slider "Hardpoint item size (fallback)", "rs_hardpoint_prop_scale_arm", 0.02, 0.5, 0.01, 2
	// The torso-scale dial. Every mount in this fork is arm-anchored, so it
	// is the slider above that does the work here -- but this cvar was READ
	// by RS_HardPointProp.holsterPropScale() while being declared in no
	// CVARINFO at all, which meant it silently ran at its hardcoded 0.18
	// fallback with no way to see or change it. Declared and given a row
	// 2026-08-26; it becomes live again when the two forks merge back.
	Slider "Torso item size (fallback)", "rs_hardpoint_prop_scale", 0.02, 1.0, 0.01, 2
	Slider "Fill amount (measured models)",  "rs_hardpoint_prop_fill", 0.1, 1.0, 0.05, 2
	Slider "Stored item yaw",           "rs_hardpoint_prop_yaw", -180.0, 180.0, 5.0, 0
	Slider "Stored item pitch",         "rs_hardpoint_prop_pitch", -180.0, 180.0, 5.0, 0
	Slider "Stored item roll",          "rs_hardpoint_prop_roll",  -180.0, 180.0, 5.0, 0
	Slider "Stored item up/down",       "rs_hardpoint_prop_up",  -12.0, 12.0, 0.5, 1
	Slider "Stored item fwd/back",      "rs_hardpoint_prop_fwd", -12.0, 12.0, 0.5, 1
	Slider "Stored item side",          "rs_hardpoint_prop_side", -12.0, 12.0, 0.5, 1
	StaticText " "

	StaticText "Gesture-Cast Arming", "Gold"
	Option "Enable gesture-cast",       "rs_hardpoint_gesture_enable", "OnOff"
	StaticText "Off by default. Arming is a roll test that a hand at rest", "DarkGray"
	StaticText "already passes, so leaving this on pins that hand's grip", "DarkGray"
	StaticText "claim permanently. Each arm arms its OWN three mounts: roll", "DarkGray"
	StaticText "palm-out and they fire in place, without ever being drawn.", "DarkGray"
	StaticText "Also needs that arm's Wrist slots on, and edit mode off.", "DarkGray"
	StaticText "Its fire keys are still PLACEHOLDER binds -- see KEYCONF.", "DarkGray"
	Slider "Palm-out roll target",      "rs_hardpoint_gesture_roll_target", -180.0, 180.0, 5.0, 0
	Slider "Palm-out roll tolerance",   "rs_hardpoint_gesture_roll_tolerance", 5.0, 90.0, 5.0, 0
	StaticText " "

	StaticText "Timing & Sound", "Gold"
	Option "Instant store/draw",       "rs_hardpoint_instant_switch", "OnOff"
	Option "Store/draw confirm sound", "rs_hardpoint_sound", "OnOff"
	Option "Confirm sound style",      "rs_hardpoint_sound_style", "RS_HardPointSoundStyle"
	StaticText " "

	// ENGINE cvar (DoomXR), not this pk3's own -- referencing it here does
	// not require this pk3 to own or declare it. Two handed weapons /
	// stabilize distance used to be listed here too, but those are general
	// VR control settings, not anything hardpoint-specific -- they already
	// have a home in the engine's own options menu, and duplicating them
	// here was just two ways to find the same setting.
	StaticText "Grip", "Gold"
	Option "Grip works holsters",       "vr_holster_use_grip", "OnOff"
	StaticText " "

	StaticText "Diagnostics", "Gold"
	SafeCommand "Debug dump to console", "netevent rs-hardpoint-debug"
	Option "Verbose console logging",    "rs_hardpoint_verbose", "OnOff"
	StaticText "Range enter/leave lines, and the automatic orientation", "DarkGray"
	StaticText "dump after every store. Off in a normal build; the", "DarkGray"
	StaticText "manual dump above and the range haptic are unaffected.", "DarkGray"
	Option "Wrist pitch live dump",      "rs_hardpoint_wristdump", "OnOff"
	StaticText "Prints each arm's raw pitch/roll/angle and its three", "DarkGray"
	StaticText "anchor positions ~7x a second, per live bank.", "DarkGray"
	StaticText "For tuning the gesture roll target against a real tilt.", "DarkGray"
}


// ============================================================
// from RS_Reload  (MENUDEF.txt)
// ============================================================
// EVERY CVAR IN THIS PACKAGE HAS A ROW HERE, and that is a hard rule rather
// than a courtesy: this is played in VR with no keyboard, so a setting with no
// menu row is a setting that does not exist.
//
// AddOptionMenu appends to the engine's own Options menu rather than replacing
// it, so this coexists with RS_Hands, RS_Holsters and anything else loaded.

AddOptionMenu "OptionsMenu"
{
	Submenu "RS Reload", "RR_Options"
}

OptionValue "RR_FeedForce"
{
	-1, "Auto"
	 0, "None (vanilla reload)"
	 1, "Box magazine"
	 2, "Bolt"
	 3, "Pump"
	 4, "Break action"
	 5, "Belt"
	 6, "Pod / tube"
	 7, "Energy cell"
}

OptionValue "RR_ArchForce"
{
	-1, "Auto"
	 0, "Pistol"
	 1, "Revolver"
	 2, "SMG"
	 3, "Rifle"
	 4, "Sniper"
	 5, "Shotgun"
	 6, "Super shotgun"
	 7, "Chaingun"
	 8, "Railgun"
	 9, "Rocket"
	10, "Grenade launcher"
	11, "Plasma"
	12, "BFG"
	13, "Unmaker"
	14, "Melee"
}

OptionMenu "RR_Options"
{
	Title "RS Reload"

	StaticText ""
	StaticText "Reloading is gestural -- no button starts it.", "Gold"
	StaticText "Your off hand arriving at the magwell is the release."
	StaticText ""

	// NOT BUILT YET, AND THE ROW SAYS SO. rr_magazines is a real cvar with a
	// real toggle and nothing behind it -- RR_Reload.Refill reads it and
	// returns. The row stays because the feature is designed and sequenced
	// (after the grip arbiter, see CLAUDE.md) and deleting it would mean
	// re-adding it; the label carries the warning because a toggle that
	// advertises a behaviour it does not have is worse than no toggle at all.
	// When Refill lands, this is the line to shorten back.
	Option "Magazines (not implemented)", "rr_magazines", "OnOff"
	StaticText "Reserved for the magazine and reserve-pool split. Does", "DarkGray"
	StaticText "nothing yet: reloading is a gesture over one flat ammo", "DarkGray"
	StaticText "pool either way, exactly as it is with this off.", "DarkGray"

	Option "Show reach dot", "rr_marks",     "OnOff"
	Slider "Ammo model size", "rr_ammo_scale", 0.05, 4.0, 0.05, 2

	StaticText " "
	StaticText "Tuning", "Gold"
	// Forcing a family makes every weapon load the same way, which is how you
	// tune one sequence at a time instead of hunting for a gun that uses it.
	Option "Force feed type", "rr_force_feed", "RR_FeedForce"
	Option "Force archetype", "rr_force_arch", "RR_ArchForce"
	Option "Trace beats",     "rr_debug",      "OnOff"

	StaticText " "
	Submenu "Weapon Sizes", "RR_Sizes"
}

// THE ONLY PER-GUN NUMBERS IN THE SYSTEM. Fourteen pairs, not two per weapon --
// tuning "Pistol" sizes every pistol in every mod that will ever load.
//
// Length is the gun in map units (a Doom player is 56 tall). Grip is how far
// forward of the controller the firing grip sits, which also absorbs the
// wrist-to-palm gap.
OptionMenu "RR_Sizes"
{
	Title "Weapon Sizes"

	StaticText ""
	StaticText "Length / grip offset, in map units.", "Gold"
	StaticText "Turn on the reach dot to see what you are moving."
	StaticText ""

	Slider "Pistol length",     "rr_len_pistol",     2, 40, 0.5, 1
	Slider "Pistol grip",       "rr_grip_pistol",   -4, 16, 0.5, 1
	Slider "Revolver length",   "rr_len_revolver",   2, 40, 0.5, 1
	Slider "Revolver grip",     "rr_grip_revolver", -4, 16, 0.5, 1
	Slider "SMG length",        "rr_len_smg",        2, 40, 0.5, 1
	Slider "SMG grip",          "rr_grip_smg",      -4, 16, 0.5, 1
	Slider "Rifle length",      "rr_len_rifle",      2, 40, 0.5, 1
	Slider "Rifle grip",        "rr_grip_rifle",    -4, 16, 0.5, 1
	Slider "Sniper length",     "rr_len_sniper",     2, 40, 0.5, 1
	Slider "Sniper grip",       "rr_grip_sniper",   -4, 16, 0.5, 1
	Slider "Shotgun length",    "rr_len_shotgun",    2, 40, 0.5, 1
	Slider "Shotgun grip",      "rr_grip_shotgun",  -4, 16, 0.5, 1
	Slider "Super SG length",   "rr_len_ssg",        2, 40, 0.5, 1
	Slider "Super SG grip",     "rr_grip_ssg",      -4, 16, 0.5, 1
	Slider "Chaingun length",   "rr_len_chaingun",   2, 40, 0.5, 1
	Slider "Chaingun grip",     "rr_grip_chaingun", -4, 16, 0.5, 1
	Slider "Railgun length",    "rr_len_railgun",    2, 40, 0.5, 1
	Slider "Railgun grip",      "rr_grip_railgun",  -4, 16, 0.5, 1
	Slider "Rocket length",     "rr_len_rocket",     2, 40, 0.5, 1
	Slider "Rocket grip",       "rr_grip_rocket",   -4, 16, 0.5, 1
	Slider "Grenade length",    "rr_len_grenade",    2, 40, 0.5, 1
	Slider "Grenade grip",      "rr_grip_grenade",  -4, 16, 0.5, 1
	Slider "Plasma length",     "rr_len_plasma",     2, 40, 0.5, 1
	Slider "Plasma grip",       "rr_grip_plasma",   -4, 16, 0.5, 1
	Slider "BFG length",        "rr_len_bfg",        2, 40, 0.5, 1
	Slider "BFG grip",          "rr_grip_bfg",      -4, 16, 0.5, 1
	Slider "Unmaker length",    "rr_len_unmaker",    2, 40, 0.5, 1
	Slider "Unmaker grip",      "rr_grip_unmaker",  -4, 16, 0.5, 1
}


// ============================================================
// from RS_WeaponWheel  (menudef.txt)
// ============================================================
// Appended verbatim. No name collides with anything above -- every menu and
// option-value here is wr_*, every one above is RS_*/RR_*/HS_*.
//
// TWO DIFFERENCES FROM THE FIVE LANES ABOVE, both kept on purpose:
//
//   It adds itself to OptionsMenuSimple as well as OptionsMenu. The five
//   above only add to OptionsMenu, so the wheel is the one entry that shows
//   up for someone browsing the simple options list. That was the wheel's own
//   decision and it is a reasonable one; the asymmetry is noted here so the
//   next person does not "fix" it by deleting the line.
//
//   Its menu and option-value names are unquoted (OptionMenu wr_Options) where
//   the lanes above quote them ("RS_HandsOptions"). MENUDEF accepts both. Not
//   normalised, because a rename here is a silent break: nothing verifies that
//   a Submenu target still exists.
//
// The Control rows near the bottom bind alias names (wr_toggle_off, wr_place,
// ...) rather than key sections, so folding the wheel's two KEYCONF sections
// into RSVRUnified left them working untouched.
// Its own page, not an addition to the engine's.
//
// The native VR wheel has a full options page of its own under vr_wheel_*, and
// this is a different system rather than a skin on that one -- different
// layout, different selection, different equip path. Sharing a page would mean
// two sets of sliders that look interchangeable and are not.
//
// Added to BOTH option menus: OptionsMenuSimple is what a lot of people
// actually browse, and a mod that only appears in the full one is a mod half
// its users never find.
AddOptionMenu OptionsMenu       { Submenu "RSVR HUD", wr_Options }
AddOptionMenu OptionsMenuSimple { Submenu "RSVR HUD", wr_Options }

OptionMenu wr_Options
{
	Title "RSVR HUD"

	StaticText ""
	StaticText "Behaviour", gold
	Option "Open at level start",       "wr_autoopen",     "OnOff"
	Slider "Seconds before it folds",   "wr_locktics",     35, 700, 35, 0
	Slider "Warning before it folds (0 = off)", "wr_warn_tics", 0, 105, 35, 0

	StaticText ""
	StaticText "Size", gold
	Slider "Overall scale",             "wr_scale",        0.3, 3.0, 0.05, 2

	StaticText ""
	StaticText "Placement", gold
	// Every one of these is live -- open the rig and drag a slider and the next
	// tic has it. That is the whole reason they are sliders and not constants.
	Slider "Ring distance ahead",       "wr_forward",      5.0, 120.0, 1.0, 0
	Slider "Ring radius",               "wr_radius",       1.0, 40.0, 0.5, 1
	Slider "Height above hand",         "wr_rise",        -8.0, 12.0, 0.5, 1
	Slider "Lean toward you",           "wr_tilt",       -30.0, 30.0, 1.0, 0
	// Off by default: a tracked controller reports ninety degrees of roll just
	// from how you hold it, so raw wrist roll swings the ring behind you.
	Slider "Wrist roll spins the ring", "wr_roll",         0.0, 1.0, 0.05, 2

	// Closes the ENGINE'S OWN native VR wheels the instant any of the three
	// tries to open -- ours already replaces two of them and the third is
	// meant to route through this mod's own gesture instead. Off if you
	// want the native wheels back, or want to compare them against this
	// mod's own.
	Option "Block the engine's native VR wheels", "wr_suppress_native_wheel", "OnOff"

	StaticText ""
	StaticText "Layout", gold
	// Two shapes for the same set of cards. A ring divides 360 degrees by its
	// card count, so every extra weapon tightens every gap; a comb grows
	// outward at a spacing that never tightens -- nineteen cells by its second
	// ring. Both keep a fixed place per slot, so either stays learnable.
	Option "Honeycomb instead of ring",   "wr_hex",        "OnOff"
	Slider "  Rings arrive apart by (tics)", "wr_hex_stagger", 0, 12, 1, 0

	StaticText ""
	StaticText "Cards", gold
	Slider "Card width",                "wr_panel_w",      1.0, 12.0, 0.25, 2
	Slider "Card height",               "wr_panel_h",      1.0, 12.0, 0.25, 2
	Slider "Hovered card steps out",    "wr_pop",          0.0, 6.0, 0.25, 2
	Slider "Hovered card pulse",        "wr_pulse",        0.0, 0.5, 0.01, 2
	// Each card aims at your eye instead of all sharing one angle -- the ring
	// becomes a shallow bowl instead of a flat sheet.
	Option "Cards face you individually", "wr_facing",     "OnOff"
	// The artwork slides inside its frame as your head moves.
	Slider "Face parallax",             "wr_parallax",     0.0, 0.25, 0.01, 2
	// A few millimetres of rise and fall while it sits there.
	Slider "Idle breathing",            "wr_idle",         0.0, 2.0, 0.05, 2
	Slider "  Breathing speed",         "wr_idle_speed",   0.2, 6.0, 0.1, 1
	Slider "Scanline crawl",            "wr_scan_crawl",   0.0, 4.0, 0.1, 1
	// 0 makes the ring appear finished, which is how this behaved before the
	// cards grew out of the wrist.
	Slider "Cards grow in over (tics)", "wr_growtics",     0, 20, 1, 0
	Option "Ammo counts and gauges",    "wr_ammo",         "OnOff"
	// Below this share of a full load a card pulses. Every weapon set gets
	// this -- it needs no rarity system to read against.
	Slider "Low ammo warns below",      "wr_lowammo_frac", 0.0, 0.9, 0.05, 2
	Slider "  Low ammo pulse",          "wr_lowammo_pulse", 0.0, 1.0, 0.05, 2
	// A slow drift of brightness across the whole ring while it sits there,
	// each card on its own offset phase so the ring twinkles rather than
	// blinking in step. Works on any weapon set, rarity or not.
	Slider "Idle shimmer",              "wr_shimmer",      0.0, 1.0, 0.05, 2
	// Off falls back to the sampled plate: cheaper, blurs up close, and cannot
	// glow -- a halo needs a distance field to read past the shape's edge.
	Option "Sharp plates (SDF)",        "wr_sdf",          "OnOff"
	// Only fires on the card under your pointer. The one thing a sampled plate
	// cannot do at all -- a halo needs a field to read past the shape edge.
	Slider "  Hover glow",              "wr_glow",          0.0, 4.0, 0.1, 1
	Slider "Corner rounding",           "wr_plate_radius", 0, 15, 1, 0
	Slider "Plate border",              "wr_plate_border", 0, 15, 1, 0
	// RS_Main's Basic tier -- the one nearly every fresh grant lands on --
	// is white by design. On keeps rarity readable at a glance; off trades
	// that for variety, falling back to the classic slot palette / a
	// per-weapon hashed hue instead of asking RS_Main at all.
	Option "Colour cards by rarity tier", "wr_tier_color", "OnOff"
	// The other way to build a card: artwork painted into a texture in 2D pixel
	// coordinates instead of assembled from billboards in map units. Both work.
	// A raster, so it shows its pixels up close where the field payloads do not.
	// Spawns the weapon own class as an inert prop so the engine resolves its
	// MODELDEF. Real geometry with real stereo depth, not a picture of a gun.
	Option "Real models on cards",      "wr_models",       "OnOff"
	Slider "  Model size",              "wr_model_scale",  0.02, 1.0, 0.02, 2
	Slider "  Model stand-off",         "wr_model_lift",   0.0, 12.0, 0.5, 1
	Option "Painted card faces",        "wr_canvas",       "OnOff"
	// Above 1 the sprite crops at the card edge, which no billboard can do.
	Slider "  Sprite bleed",            "wr_canvas_bleed",  0.6, 2.5, 0.05, 2
	Option "  Sprite shadow",           "wr_canvas_shadow", "OnOff"
	Slider "  Scanlines",               "wr_canvas_scan",   0.0, 0.8, 0.02, 2

	StaticText ""
	StaticText "Data sheet", gold
	// The panel in the middle of the ring. Off returns the centre to empty
	// space and lets the ring shrink back to its own radius -- the sheet holds
	// a minimum open so cards orbit clear of it.
	Option "Show data sheet",           "wr_sheet",        "OnOff"
	// Measured in card widths, so it already follows wr_panel_* and wr_scale.
	// Past about 1.5 the ring is being sized by the sheet, not by your cards.
	Slider "  Sheet size",              "wr_sheet_scale",  0.5, 2.0, 0.05, 2
	// Tier, condition, DPS, accuracy, crit, velocity, rate of fire and the
	// real magazine size, read straight off the weapon. Off leaves the rows
	// the engine itself can answer, which work with no weapon mod at all.
	Option "  Rolled weapon stats",     "wr_sheet_stats",  "OnOff"
	// Kills, shots, accuracy, time held, and headshots when RS_Headshots is
	// loaded -- counted by this mod as you play rather than read off another
	// mod, so it works on every weapon including plain vanilla ones.
	Option "  Track kills and accuracy", "wr_stats_track", "OnOff"

	StaticText ""
	StaticText "Inspect a weapon in the world", gold
	// Point at a gun on the floor and the same sheet appears for it, with
	// how it compares to what that hand is already holding.
	Option "Show card when pointing at a weapon", "wr_inspect", "OnOff"
	Slider "  Rest on it for (tics)",   "wr_inspect_dwell", 0, 70, 2, 0
	Option "  Compare against held weapon", "wr_inspect_delta", "OnOff"
	Submenu "  Weapon mod compatibility...", wr_Compat

	StaticText ""
	StaticText "The gun's own readout", gold
	// A count welded to the weapon in your hand, separate from the ring's own
	// cards -- it is there whether the ring is open or not.
	Option "Show it",                   "wr_gun",          "OnOff"
	Option "  On the off-hand weapon too", "wr_gun_offhand", "OnOff"
	// Flush reads as part of the gun and needs a weapon measured to look
	// right; facing you is always readable and needs nothing measured.
	Option "  Placement",               "wr_gun_face",     "wr_GunFacing"
	Option "  Which side of the gun",   "wr_gun_side",     "wr_GunSides"
	Option "  LCD instead of LED",      "wr_gun_lcd",      "OnOff"
	Slider "  Glow",                    "wr_gun_glow",     0.0, 2.0, 0.05, 2
	// The one placement nobody has measured falls back to this -- one guess,
	// not a table of them, and it is honestly approximate rather than quietly
	// wrong. wr_place fixes the guns that actually matter.
	Slider "  Fallback forward",        "wr_gun_f",       -4.0, 16.0, 0.25, 2
	Slider "  Fallback left",           "wr_gun_l",       -8.0, 8.0, 0.25, 2
	Slider "  Fallback up",             "wr_gun_u",       -8.0, 8.0, 0.25, 2
	Slider "  Fallback width",          "wr_gun_w",        1.0, 16.0, 0.25, 2
	Slider "  Fallback height",         "wr_gun_h",        1.0, 10.0, 0.25, 2
	// Source cannot answer this one -- it depends on which way your actual
	// runtime reports wrist roll. Flip it if the readout rolls backwards when
	// you turn the gun over.
	Option "  Roll follows wrist as",   "wr_gun_rollsign", "wr_RollSign"
	Submenu "  Placement tool...",      wr_Place

	StaticText ""
	StaticText "The room", gold
	// All four borrow a single global engine slot each. A co-loaded mod using
	// the same slot gets it back the moment the rig closes.
	//
	// The switches live here; the shaping numbers behind each one are a page
	// down, so turning an effect on does not mean scrolling past nine sliders
	// that only matter once you have decided you like it.
	Option "Laser has dust in it",      "wr_vbeam",        "OnOff"
	Slider "Opening light sweep (tics)","wr_sweep",        0, 40, 1, 0
	Slider "Mist density",              "wr_fog",          0.0, 1.0, 0.05, 2
	// A mark under your feet, repeated around a circle and spinning. 0 is off.
	Slider "Floor marks (size)",        "wr_floor",        0.0, 40.0, 1.0, 0
	Slider "Floor mark count",          "wr_floor_count",  1.0, 24.0, 1.0, 0
	Slider "Floor mark spin",           "wr_floor_spin", -60.0, 60.0, 2.0, 0
	// Ripples the glows a map ALREADY has. In a map with none it does nothing
	// at all -- off by default, and not broken when it looks dead.
	Slider "Room glow ripple",          "wr_wave",         0.0, 3.0, 0.1, 1
	Submenu "Fine tuning...",           wr_RoomDetail

	// Below this many weapons: every one gets its own card, no dwell needed.
	// At or past it: one card per slot, bearings stay learnable, and a card
	// hiding others marks itself with a small "+N".
	Slider "Flat ring up to (weapons)", "wr_subcards_max", 0, 30, 1, 0
	// Tics the laser can spend on another real card before an open fan
	// actually folds -- 0 is instant, the old behaviour.
	Slider "Fan close forgiveness",     "wr_subcards_grace", 0, 20, 1, 0
	// What a crowded slot unfolds INTO. Off is the fan -- three tidy lanes
	// stacked outward. On scatters the same cards as stars around the one you
	// opened and draws a line out to each. The scatter is fixed per slot, so
	// a slot opens to the same shape every time rather than reshuffling.
	Option "Constellation instead of fan", "wr_constellation", "OnOff"
	Slider "  Lines draw apart by (tics)", "wr_star_stagger", 0, 12, 1, 0
	Slider "Unhovered cards fade to",   "wr_dim",          0.05, 1.0, 0.05, 2
	Option "Mark weapons you hold",     "wr_marker",       "OnOff"
	Option "Slot number on cards",      "wr_slotnum",      "OnOff"

	// Separates a card from a busy wall without knowing anything about the wall.
	Slider "Card shadow",               "wr_shadow",       0.0, 1.0, 0.05, 2
	Slider "  Shadow offset",           "wr_shadow_offset", 0.0, 1.5, 0.05, 2
	Option "Ammo readout",              "wr_readout",     "wr_Readouts"
	// ONE light, on the hovered card only -- eight would be eight overlapping
	// volumes lighting the same wall.
	Option "Hovered card lights room",  "wr_light",        "OnOff"
	Slider "  Light size",              "wr_light_size",   8.0, 160.0, 4.0, 0
	// Tics the light survives crossing the gap between two cards. 0 cuts it
	// the instant you leave a card, which reads as a flicker while browsing.
	Slider "  Light holds across gaps", "wr_light_grace",  0, 12, 1, 0

	// Quiet points of light behind the cards, further back than they are, so
	// head movement separates them into their own plane. Same sky every time
	// rather than a fresh scatter each summon. 0 is off.
	Slider "Background stars",          "wr_stars",        0, 200, 10, 0
	Slider "  Star brightness",         "wr_stars_alpha",  0.0, 1.0, 0.05, 2

	StaticText ""
	StaticText "Taking a weapon", gold
	// A one-shot ring at the card you took, plus the same flash when a fan
	// opens. Reads even with sound and rumble both off.
	Option "Flash on confirm",          "wr_flash",        "OnOff"
	Slider "Sparks",                    "wr_sparks",       0.0, 80.0, 2.0, 0
	Slider "Spark speed",               "wr_sparks_speed", 0.2, 4.0, 0.1, 1
	Option "Spark colour",              "wr_spark_color", "wr_SparkColors"
	// Roll is a real billboard axis now, and the fold-away keeps the card alive
	// long enough to see it turn.
	Slider "Taken card spins",          "wr_flip",      -720.0, 720.0, 30.0, 0

	StaticText ""
	StaticText "Feel", gold
	// The rig is worn where you cannot comfortably look at it, so the ear and
	// the wrist do most of the work of saying what just happened.
	Option "Sound",                     "wr_sound",        "OnOff"
	Slider "Volume",                    "wr_volume",       0.0, 1.0, 0.05, 2
	// vr_enable_haptics still overrules this, so turning rumble off in the
	// engine cannot be undone from here.
	Slider "Controller rumble",         "wr_haptics",      0.0, 2.0, 0.1, 1

	StaticText ""
	StaticText "Reach", gold
	// Depth tolerance more than reach: how far in front of or behind the grid
	// your hand may sit and still register.
	// 0 turns reaching off and leaves the beam as the only way in. Drop
	// wr_forward toward 0 to bring the ring into actual arm reach.
	Slider "Reach-in radius (0 = off)", "wr_touch",        0.0, 20.0, 0.5, 1
	Slider "Stick deadzone",            "wr_stickdead",    0.05, 0.9, 0.05, 1

	StaticText ""
	StaticText "Integration", gold
	Option "Slow time while open (BulletTimeX)", "wr_bullettime", "OnOff"

	StaticText ""
	StaticText "Controls", gold
	// Whichever hand you summon it on is the hand that wears it, points at it,
	// and receives what you pick -- so the bind already says which hand you
	// meant and there is no handedness setting to get wrong.
	Control "Off hand wheel",           "wr_toggle_off"
	Control "Main hand wheel",          "wr_toggle_main"
	// Both binds still work as their own keys once a ring is open --
	// this only decides which hand an EMPTY-handed press opens on, for
	// a player who always means the same one regardless of which key
	// they actually reached for.
	Option "Always open on",            "wr_toggle_prefer_hand", "wr_ToggleHandPref"
	// Pressing the same hand's key again just after opening recenters the
	// ring on your hand instead of closing it -- 0 turns this off, the key
	// always closes.
	Slider "Recenter window, tics (0 = off)", "wr_recenter_window", 0, 35, 5, 1

	StaticText ""
	StaticText "Troubleshooting", gold
	Option "Print diagnostics on open", "wr_debug",        "OnOff"

	StaticText ""
}

// THE PLACEMENT TOOL, for measuring where a readout sits on a given weapon.
//
// Its own page and its own key section because this is a calibration
// workflow, not a normal-play control -- but reachable WITHOUT a console,
// because this mod is used in VR and there is no console there to reach.
OptionMenu wr_Place
{
	Title "RSVR HUD - PLACEMENT TOOL"

	StaticText ""
	StaticText "Which hand is being measured", gold
	// The OTHER hand is always the cursor, so measuring an offhand weapon
	// means flipping this first.
	Option "Calibrating",               "wr_gun_placehand", "wr_PlaceHand"

	StaticText ""
	StaticText "Nudge step per press", gold
	Slider "Distance",                  "wr_gun_step",     0.05, 2.0, 0.05, 2
	Slider "Angle",                     "wr_gun_astep",    0.5, 15.0, 0.5, 1

	StaticText ""
	StaticText "Controls", gold
	Control "Enter/exit placement mode", "wr_place"
	Control "Lock touch point",          "wr_lock"
	Control "Two-point size measure",    "wr_measure"
	Control "Print calibration line",    "wr_save"

	StaticText ""
}

OptionValue wr_PlaceHand
{
	0, "Main hand"
	1, "Off hand"
}

// WHAT THE DATA SHEET READS OUT OF OTHER WEAPON MODS.
//
// Its own page because these are nine switches that only matter to whoever
// has that particular mod loaded -- on the main page they would be nine rows
// of noise for everyone else. Every one is ON by default and costs nothing
// when the mod it names is absent: the sheet asks whether a field exists on
// the weapon in your hand, and a mod that is not loaded simply has no field
// to find, so the row never appears. Turning one off here is for when you
// have the mod AND would rather not see its rows.
OptionMenu wr_Compat
{
	Title "RSVR HUD - WEAPON MOD DATA"

	StaticText ""
	StaticText "Mods with a rarity ladder", gold
	// These three also COLOUR the card and the sheet title by their own
	// rarity, which is why each has a separate switch for its extra rows:
	// you may want the colour without the detail.
	Option "LegenDoom rarity",          "wr_ld_compat",    "OnOff"
	Option "  Rolled effects row",      "wr_ld_effects",   "OnOff"
	Option "DoomRL Arsenal tier",       "wr_drla_compat",  "OnOff"
	Option "  Mod Station upgrades row", "wr_drla_mods",   "OnOff"
	// Rarity, plus your character level, XP, Inferno level and rolled stats.
	Option "Doomablo rarity and level", "wr_dbl_compat",   "OnOff"
	// Rolled damage/rate/spread/pellets/crit, exotic ammo, fire modes, alt
	// modules, quirks, overheat and the run itself. Its stats are readable on
	// a weapon still on the floor, so this is the mod the inspect card and
	// the comparison table were really built for.
	Option "DOOM Infinite weapon rolls", "wr_di_compat",   "OnOff"

	StaticText ""
	StaticText "Mods with no rarity ladder", gold
	// None of these six colour anything -- they have no tier concept at all,
	// confirmed by survey rather than assumed. They only add rows.
	//
	// One switch for the whole Pandemonia family: base game durability,
	// magazine and sidegrades, Anarchy's Anarchic Sigil levelling, and
	// Insurrection's augments, durability and charge bars.
	Option "Pandemonia family",         "wr_pand_compat",  "OnOff"
	// Spell cooldown and the five player resource pools.
	Option "Guncaster resources",       "wr_gc_compat",    "OnOff"
	// Plasma rifle heat, and Unmaker demon keys.
	Option "MetaDoom heat and keys",    "wr_meta_compat",  "OnOff"
	// Damage, accuracy, rate of fire, recoil, clip and rolled level -- read
	// from the array BorderDoom's own stat lookup writes into, never from
	// that lookup itself, which has side effects on your ammo capacity.
	Option "BorderDoom weapon stats",   "wr_bd_compat",    "OnOff"
	// All four classes and their meters -- BlastMaster heat (which also
	// takes the sheet's gauge), Artificer skill and mace, the Tech Monk's
	// Striker and Kame Bomb, Past Linked's subweapon carousel and its three
	// upgrade ladders. Includes the warning the game itself only gives you
	// once: which upgrades vanish when you leave the level.
	Option "Combined Arms class meters", "wr_ca_compat",   "OnOff"
	// Nine weapon sets, one active at a time. Coverage is uneven on purpose
	// -- Whitemare, Hellbound, JPCP, Alien Vendetta and BTSX have real live
	// counters worth a row; Doom II and Aliens TC are fixed math with
	// nothing to read past which set is active.
	Option "Final Doomer set mechanics", "wr_fd_compat",   "OnOff"
	// Seven characters, 48 weapons. Magazines, mana, heat, charge, spin-up
	// and status effects. Nothing reads off a Lithium weapon on the floor --
	// that mod deletes unowned weapon actors.
	// Ours, not a third party -- a real two-way bridge, not a guess.
	// Cycle keys are under the main RSVR HUD key section, unbound by
	// default.
	Option "ModelSwapper model on card", "wr_ms_compat",  "OnOff"
	Option "Lithium weapons",           "wr_lith_compat", "OnOff"
	// Separate switch, because this is the only setting in the whole mod that
	// calls another mod's own scripts rather than reading its data. Five gates
	// sit in front of the call and the script was disassembled to prove it
	// cannot mutate anything -- but turn this off if a Lithium update lands
	// before an update to this, or if those rows ever look wrong.
	Option "  Lithium RPG stats (ACS)", "wr_lith_acs",    "OnOff"

	StaticText ""
}

// The seven SDF shape kinds, by name rather than by number. wr_floor_kind is a
// float cvar, so the keys are floats -- an int key silently matches nothing and
// the option renders blank.
OptionValue wr_ShapeKinds
{
	1.0, "Disc"
	2.0, "Ring"
	3.0, "Square"
	4.0, "Square outline"
	5.0, "Cross"
	6.0, "Hexagon"
	7.0, "Triangle"
}

// Everything behind the four switches on the main page.
//
// Separated because these are the numbers you touch AFTER deciding you want an
// effect, not while deciding. Nothing here is console-only -- that is the whole
// point of the page.
OptionMenu wr_RoomDetail
{
	Title "RSVR HUD - ROOM DETAIL"

	StaticText ""
	StaticText "Laser volume", gold
	// A raymarched cone laid along the ray the engine laser already points
	// down. Inner is the hot core, outer where it has faded to nothing.
	Slider "Core radius",               "wr_vbeam_inner",   0.0, 8.0, 0.1, 1
	Slider "Outer radius",              "wr_vbeam_outer",   0.5, 20.0, 0.5, 1
	Slider "Density",                   "wr_vbeam_density", 0.0, 2.0, 0.05, 2
	// Motes drifting in the beam. 0 is a clean cone.
	Slider "Dust",                      "wr_vbeam_dust",    0.0, 2.0, 0.05, 2

	StaticText ""
	StaticText "Opening sweep", gold
	// Tested per pixel against world position, so it crosses floor, wall and
	// ceiling as one unbroken line rather than lighting whole sectors.
	Slider "Travel distance",           "wr_sweep_reach",  40.0, 900.0, 20.0, 0
	Slider "Brightness",                "wr_sweep_bright",  0.0, 4.0, 0.1, 1

	StaticText ""
	StaticText "Room glow ripple", gold
	// Wavelength, how fast it travels, and how tight the peaks are.
	Slider "Wavelength",                 "wr_wave_len",    20.0, 400.0, 10.0, 0
	Slider "Speed",                      "wr_wave_speed",  -6.0, 6.0, 0.2, 1
	Slider "Sharpness",                  "wr_wave_sharp",   0.2, 6.0, 0.1, 1

	StaticText ""
	StaticText "Mist", gold
	// The slab has a TOP, so this is how far above the ring its surface sits.
	Slider "Surface height above ring",  "wr_fog_rise",   -20.0, 40.0, 1.0, 0
	// Lets the laser cone light the mist it passes through.
	Slider "Scatter",                    "wr_fog_scatter",  0.0, 2.0, 0.05, 2

	StaticText ""
	StaticText "Floor marks", gold
	Option "Shape",                      "wr_floor_kind",  "wr_ShapeKinds"
	Slider "Ring radius",                "wr_floor_space", 10.0, 200.0, 2.0, 0
	Slider "Line thickness",             "wr_floor_thick",  0.02, 1.0, 0.02, 2
	Slider "Brightness",                 "wr_floor_bright", 0.0, 4.0, 0.1, 1
	// Splits each mark down its middle as the ring arrives, showing the under
	// colour through the gap.
	Slider "Split open by",              "wr_floor_seam",   0.0, 1.0, 0.05, 2

	StaticText ""
}

// Slot colour first because it is the one that carries information: the burst
// comes out the same hue as the card, the accent and the beam, so it confirms
// which one you hit. The other two are decoration.
OptionValue wr_SparkColors
{
	0, "Slot colour"
	1, "White hot"
	2, "Rainbow"
}

// LED first because it is the one that survives a dark room, which is where
// this thing mostly gets used.
OptionValue wr_Readouts
{
	0, "LED segments"
	1, "LCD, punched out"
	2, "Lozenge badge"
}

OptionValue wr_GunFacing
{
	0, "Flush on the weapon"
	1, "Facing you"
}

OptionValue wr_GunSides
{
	0, "Side A"
	1, "Side B"
}

OptionValue wr_RollSign
{
	1,  "Forward"
	-1, "Reversed"
}

OptionValue wr_ToggleHandPref
{
	-1, "Whichever bind you press"
	0,  "Main hand"
	1,  "Off hand"
}
