> ## ⚠ DO NOT LOAD THIS ALONGSIDE `RS_VR_Unified.pk3`
>
> The merged build already contains `RS_GripArbiterService`. Loading both
> declares the class twice, which is `Class name RS_GripArbiterService already
> exists` -> `I_Error` -> **every pk3 after it refuses to compile**. The whole
> family fails to start, and nothing in the log points here.
>
> Use the merge, **or** this pk3 plus the individual mods. Never both.

merge of Hands, Holsters, HardPoints, Grabbing, Gestures, Distance Grabbing, Model-Frame Reloading, Headshots, WeaponWheelz

meant to serve as a centralized VR_Doom interface 
